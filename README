## Open WebUI Tools

This repo documents how I configure [Open WebUI](https://github.com/open-webui/open-webui) using tools and how I create my own custom tools.  
It is meant as a reference for myself and for anyone who wants to reuse or learn from my setup.

---

### Goals of this project

- **Centralize configuration**: Keep all my Open WebUI tool definitions and configuration in one place.
- **Share custom tools**: Show how I build and organize my own tools (Python/Node/script-based, etc).
- **Document my workflow**: Explain how I use these tools day‑to‑day in Open WebUI.

---

### Project structure

_Currently:_

- **`README`**: This file. High-level documentation of the project and tools.

_Current/typical layout:_

- **`tools/`**: Source code for my custom tools.
  - **`youtube_transcript.py`**: Tool to fetch YouTube transcripts.
- **`config/`**: Open WebUI tool configuration files (YAML/JSON/etc).
- **`examples/`**: Example calls, prompts, and usage notes.

I will update this section as the repo grows.

---

### My Open WebUI setup

> This section explains in general how I run Open WebUI and where these tools fit.

- **Open WebUI version**: `TODO: fill in (e.g. v0.3.x)`
- **How I run it**:
  - **Option A**: Docker
  - **Option B**: Local install (Python / Node)
- **Where tools are registered**:
  - `TODO: describe where you point Open WebUI to your tools (e.g. config file, environment variables, or UI settings).`

Example (if using Docker + config file):

docker run -d \
  -p 3000:3000 \
  -v $PWD/config:/app/config \
  -v $PWD/tools:/app/tools \
  --name open-webui \
  ghcr.io/open-webui/open-webui:latest---

### Tool configuration overview

I use Open WebUI tools to:

- **Integrate external APIs** (e.g. GitHub, Notion, etc).
- **Automate local tasks** (file operations, project scaffolding, scripts).
- **Access personal data** (within safe boundaries and with configuration in this repo).

Typical components of a tool:

- **Definition/config file** (YAML/JSON) that Open WebUI reads.
- **Implementation file** (Python/JS/etc) that does the actual work.
- **Environment variables/secrets** (API keys, tokens) that are **not** stored in this repo.

---

### Python tool: YouTube transcript (`youtube_transcript.py`)

- **Purpose**: Fetch transcript from YouTube videos (prefer Vietnamese, fallback to English if needed).
- **File**: `tools/youtube_transcript.py`
- **Required library**:

```bash
pip install youtube-transcript-api
```

- **Main function**:
  - **`get_youtube_transcript(url_or_id: str, language: str = "vi,en")`**
  - Input: YouTube URL or video ID.
  - Returns: dict containing `video_id`, `language`, `segments` (list of transcript segments) and `full_text` (full text).

- **Quick CLI test**:

```bash
python tools/youtube_transcript.py "https://www.youtube.com/watch?v=VIDEO_ID" --language vi
```

- **Open WebUI integration idea** (conceptual example):
  - Create a tool config pointing to module/function `get_youtube_transcript`.
  - In chat, you can ask:  
    "Get the transcript of this YouTube video and summarize it: https://www.youtube.com/watch?v=..."
  - Open WebUI calls the tool, gets `full_text`, then the model will summarize it.

---

### How I organize and add new tools

- **Naming**:
  - **Descriptive and short** (e.g. `github_issues_fetcher`, `project_scaffold`, `notion_notes_search`).
- **Location**:
  - Put implementations under `tools/...` by language.
  - Put configuration under `config/...`.
- **Version control**:
  - Keep **code and configuration** in git.
  - Keep **secrets and API keys** only in local `.env` files, environment variables, or secret managers (never committed).

Typical steps to add a new tool:

1. **Create the implementation file** in `tools/...`.
2. **Create/update the tool config** in `config/...`.
3. **Restart/reload Open WebUI** so it picks up the new tool.
4. **Test from the UI** and document usage in this README or an `examples/` file.

---

### Environment variables and secrets

I do **not** commit any secrets to this repo.

- **.env files**: Stored locally only (ignored by git).
- **Examples**:
  - `TODO: list example variable names, like GITHUB_TOKEN, NOTION_TOKEN, etc.`

If you want to replicate my setup:

1. Copy my sample env file (once I add it): `cp .env.example .env`.
2. Fill in your own API keys and secrets.
3. Restart Open WebUI.

---

### Usage examples

I will collect example conversations and prompt + tool sequences here over time.

Planned examples:

- **`github_issues_fetcher`**:
  - Ask: “List open issues assigned to me in repo X.”
- **`project_scaffold`**:
  - Ask: “Create a basic structure for a new Python CLI project.”

Each example will show:

- The **prompt** I type.
- The **tool** that gets called.
- The **response** and how I use it.

---

### Roadmap

What I plan to add to this repo:

- **More concrete tool examples** with real code.
- **Configs for multiple environments** (local/dev/prod).
- **Best practices** for:
  - Tool naming and organization.
  - Error handling and logging inside tools.
  - Security (limiting what tools can access).

---

### Contributing / Reuse

This repo primarily documents **my personal Open WebUI setup**, but you are welcome to:

- **Copy/adapt** tools for your own environment.
- **Open issues/PRs** if you see improvements.
- **Share feedback** on how you use similar tools.

---

### License
MIT